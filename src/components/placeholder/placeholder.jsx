import React from 'react';

/**
 * Renders a big question mark inside
 * a charachter card if no props are passed
 */
const Placeholder = () => (
  <div style={{ height: '300px', textAlign: 'center' }}>
    <h1 style={{ fontSize: '200px' }}>
      ?
    </h1>
  </div>
);

export default Placeholder;
